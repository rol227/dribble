<h1>New Org</h1>

<%= form_for @organization do |f| %>
	<p>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.submit 'Create Organization' %>
	</p>
<% end %>

<%= link_to 'Organizations', root_path %>